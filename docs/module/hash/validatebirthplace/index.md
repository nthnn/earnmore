---
title: validateBirthPlace
kind: function
longname: module:hash.validateBirthPlace
description: Validates that birthPlace contains only letters and spaces. Throws a descriptive error if it doesn't.
---

# validateBirthPlace

<Signature code="validateBirthPlace(birthPlace: string): void" />

<SourceLink href="/source/hash-ts/#L24" label="hash.ts:24" />

Validates that birthPlace contains only letters and spaces. Throws a descriptive error if it doesn't.

**Parameters**

- `birthPlace` (string)

**Returns**

- `void`
